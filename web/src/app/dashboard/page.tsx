import { ActionButton } from "@/components/action-button";
import { LogoutButton } from "@/components/logout-button";
import { getDashboardData, type AuditLog, type DashboardSummary, type HseOverview, type WorkOrder } from "@/lib/api";
import { cookies } from "next/headers";
import { redirect } from "next/navigation";

function value(number: number | undefined, suffix = "") { return number === undefined ? "--" : `${number}${suffix}`; }
function age(timestamp?: string) { if (!timestamp) return "Awaiting backend data"; const minutes = Math.max(0, Math.round((Date.now() - new Date(timestamp).getTime()) / 60000)); return minutes < 1 ? "Just now" : `${minutes} min ago`; }
function Card({ label, amount, detail, tone = "amber" }: { label: string; amount: string; detail: string; tone?: string }) { return <article className={`summary-card tone-${tone}`}><div className="card-label"><span className="pulse-dot" />{label}</div><strong>{amount}</strong><span className="card-detail">{detail}</span></article>; }
function Empty({ children }: { children: string }) { return <div className="empty-state">{children}</div>; }

export default async function DashboardPage() {
  const session = (await cookies()).get("maintainnexus_token");
  if (!session) redirect("/login");
  const [summaryResponse, orderResponse, auditResponse, hseResponse] = await getDashboardData(session.value);
  const summary: DashboardSummary | null = summaryResponse.data;
  const orders: WorkOrder[] = orderResponse.data ?? [];
  const auditLogs: AuditLog[] = auditResponse.data ?? [];
  const hse: HseOverview | null = hseResponse.data;
  const connected = summaryResponse.available || orderResponse.available || auditResponse.available || hseResponse.available;
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">M</span><span>MAINTAIN<span className="brand-accent">NEXUS</span></span></div><div className="workspace-label">Operations workspace</div><nav><a className="nav-item active" href="#overview"><span>+</span>Overview</a><a className="nav-item" href="#work-orders"><span>[]</span>Work orders <b>{summary?.open_work_orders ?? "--"}</b></a><a className="nav-item" href="#equipment"><span>O</span>Equipment</a><a className="nav-item" href="/executive"><span>$</span>Executive view</a><a className="nav-item" href="#audit"><span>#</span>Audit log</a></nav><div className="sidebar-footer"><div className="user-chip"><span className="avatar">ER</span><span><strong>Engineer view</strong><small>Station scope</small></span><span>v</span></div></div></aside><main className="main-content"><header className="topbar"><div><span className="eyebrow">CONTROL ROOM / ENGINEER</span><h1>Station overview</h1></div><div className="topbar-actions"><span className={`connection ${connected ? "connected" : "offline"}`}><i />{connected ? "API connected" : "API unavailable"}</span><span className="session-label">Engineer session</span><LogoutButton /><span className="top-avatar">ER</span></div></header><div className="content-wrap" id="overview">{!connected && <div className="notice"><strong>Waiting for the MaintainNexus API.</strong> Start FastAPI to load station-scoped telemetry and work orders.</div>}<section className="section-heading"><div><span className="eyebrow">LIVE OPERATIONS</span><h2>Good morning, engineer.</h2></div><span className="timestamp">Last sync {age(summary?.recent_health_checks?.[0]?.checked_at)}</span></section><section className="summary-grid"><Card label="System uptime" amount={value(summary?.uptime_percentage, "%")} detail="Backend-reported availability" tone="green" /><Card label="Open work orders" amount={value(summary?.open_work_orders)} detail="Awaiting action or execution" /><Card label="Active alerts" amount={value(summary?.alert_count)} detail={`${summary?.incident_count ?? "--"} equipment incidents`} tone="red" /><Card label="Downtime tracked" amount={summary?.downtime_minutes === undefined ? "--" : `${summary.downtime_minutes}m`} detail="From current work order set" tone="blue" /></section><section className="dashboard-grid bento-primary"><article className="panel" id="work-orders"><div className="panel-header"><div><span className="eyebrow">ACTION REQUIRED</span><h3>Work order queue</h3></div><span className="panel-link">All work orders -&gt;</span></div>{orders.length === 0 ? <Empty>No work orders returned by the API.</Empty> : <div className="order-list">{orders.slice(0, 5).map((order) => <div className="order-row" key={order.work_order_id}><span className="order-indicator" /><div className="order-main"><strong>{order.work_order_id}</strong><span>{order.equipment_id} / {order.reserved_part ?? "Part not supplied"}</span>{order.risk_drivers?.length ? <div className="risk-drivers"><small>Why flagged</small>{order.risk_drivers.map((driver) => <span key={driver.feature}>{driver.feature.replaceAll("_", " ")}: {driver.reason}</span>)}</div> : null}</div><span className="status">{order.status?.replaceAll("_", " ") ?? "UNKNOWN"}</span>{order.status === "PENDING_APPROVAL" ? <div className="row-actions"><ActionButton action="approve" workOrderId={order.work_order_id} /><ActionButton action="reject" workOrderId={order.work_order_id} /></div> : <span className="resolved-label">No action needed</span>}</div>)}</div>}</article><article className="panel" id="equipment"><div className="panel-header"><div><span className="eyebrow">TELEMETRY</span><h3>Equipment health</h3></div><span className="live-label"><i />Live</span></div>{summary?.recent_health_checks?.length ? <div className="health-list">{summary.recent_health_checks.slice(0, 4).map((check) => <div className="health-row" key={`${check.equipment_id}-${check.checked_at}`}><span className="health-ring" /><div><strong>{check.equipment_id ?? "Unknown equipment"}</strong><span>{check.health_status ?? "Status unavailable"}</span></div><div className="health-metric"><small>Risk</small><b>{check.risk_probability === undefined ? "--" : `${Math.round(check.risk_probability * 100)}%`}</b></div></div>)}</div> : <Empty>No telemetry checks returned by the API.</Empty>}</article></section>{hse && <section className="hse-band"><div className="hse-heading"><span className="eyebrow">HSE EARLY WARNING / SPILL PREVENTION</span><h2>Loading safety monitor</h2><p>Fill and flow analytics designed to stop overfill before an environmental incident.</p></div><div className={`hse-severity severity-${hse.severity.toLowerCase()}`}><span className="severity-kicker">{hse.tank_id} / {hse.source}</span><strong>{hse.severity}</strong><span>{hse.recommended_action}</span></div><div className="hse-metrics"><div><small>Tank level</small><b>{hse.level_percent}%</b><i><em style={{ width: `${hse.level_percent}%` }} /></i></div><div><small>Flow rate</small><b>{hse.flow_rate_lpm.toLocaleString()} L/min</b><i><em style={{ width: `${Math.min(100, hse.flow_rate_lpm / hse.max_flow_rate_lpm * 100)}%` }} /></i></div><div><small>Time to safe limit</small><b>{hse.minutes_to_safe_limit === null ? "--" : `${hse.minutes_to_safe_limit} min`}</b></div></div><div className="hse-drivers">{hse.drivers.map((driver) => <span key={driver}>+ {driver}</span>)}</div></section>}<section className="dashboard-grid lower-grid"><article className="panel" id="audit"><div className="panel-header"><div><span className="eyebrow">TRACEABILITY</span><h3>Recent audit activity</h3></div><span className="integrity"><i />Backend status</span></div>{auditLogs.length === 0 ? <Empty>No audit entries returned by the API.</Empty> : <div className="audit-list">{auditLogs.slice(0, 4).map((log) => <div className="audit-row" key={log.id}><span className="audit-marker" /><div><strong>{log.event_name.replaceAll("_", " ")}</strong><span>{age(log.timestamp)}</span></div></div>)}</div>}</article><article className="panel snapshot-panel"><div className="panel-header"><div><span className="eyebrow">STATION SNAPSHOT</span><h3>Readiness</h3></div><span className="snapshot-value">{value(summary?.uptime_percentage, "%")}</span></div><div className="readiness-bar"><span style={{ width: `${summary?.uptime_percentage ?? 0}%` }} /></div><p>Availability is rendered from <code>/api/v1/dashboard/summary</code>.</p></article></section></div></main></div>;
}
