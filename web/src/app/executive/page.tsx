import Link from "next/link";
import { getExecutiveSummary } from "@/lib/api";
import { requireSession } from "@/lib/session";
import { LiveUpdates } from "@/components/live-updates";
import { LogoutButton } from "@/components/logout-button";

export default async function ExecutivePage() {
  const { token, user } = await requireSession(["executive", "supervisor"]);
  const result = await getExecutiveSummary(token);
  const summary = result.data;
  const trend = Array.isArray(summary?.uptime_trend) ? summary.uptime_trend : [];
  return <main className="executive-shell"><header className="executive-header"><div><span className="eyebrow">VALUE COMMAND / EXECUTIVE</span><h1>Maintenance impact</h1><p>Backend-reported outcomes across the operating network.</p></div><div className="executive-actions">{user.role === "supervisor" && <Link href="/dashboard">Supervisor workspace ?</Link>}<Link href="/audit">Audit records</Link><LiveUpdates /><LogoutButton /></div></header>{!result.available && <p className="notice">Executive summary unavailable.</p>}<section className="executive-cards"><article><span>Downtime avoided</span><strong>{summary?.downtime_avoided_minutes === undefined ? "?" : `${summary.downtime_avoided_minutes}m`}</strong><small>{summary?.downtime_avoided_minutes === undefined ? "Not supplied by the backend" : "Backend-reported avoided downtime"}</small></article><article><span>Cost saved</span><strong>{summary?.cost_saved === undefined ? "?" : `$${summary.cost_saved.toLocaleString()}`}</strong><small>{summary?.cost_saved === undefined ? "Not supplied by the backend" : "Backend aggregation"}</small></article><article><span>Uptime trend</span><strong>{trend.length ? `${trend.at(-1)?.uptime}%` : typeof summary?.uptime_trend === "string" ? summary.uptime_trend.replaceAll("_", " ") : "?"}</strong><small>Backend reporting period</small></article></section>{summary?.downtime_minutes !== undefined && <section className="executive-panel"><h2>Recorded downtime</h2><strong>{summary.downtime_minutes} minutes</strong><p>{summary.open_windows ?? "?"} open windows ? {summary.completed_windows ?? "?"} completed windows. Recorded downtime is separate from avoided downtime.</p></section>}<section className="executive-panel"><h2>Uptime history</h2>{trend.length ? <figure><svg className="uptime-chart" viewBox="0 0 600 150" role="img" aria-label="Backend-reported uptime over time"><polyline fill="none" stroke="currentColor" strokeWidth="3" points={trend.map((point, index) => `${trend.length === 1 ? 300 : 20 + index / (trend.length - 1) * 560},${130 - Math.max(0, Math.min(100, point.uptime)) / 100 * 110}`).join(" ")} />{trend.map((point, index) => <circle key={`${point.date}-${index}`} cx={trend.length === 1 ? 300 : 20 + index / (trend.length - 1) * 560} cy={130 - Math.max(0, Math.min(100, point.uptime)) / 100 * 110} r="4"><title>{point.date}: {point.uptime}%</title></circle>)}</svg><figcaption>Uptime percentage, 0?100%</figcaption><table><thead><tr><th>Date</th><th>Uptime</th></tr></thead><tbody>{trend.map((point, index) => <tr key={`${point.date}-${index}`}><td>{point.date}</td><td>{point.uptime}%</td></tr>)}</tbody></table></figure> : <p className="executive-empty">The backend has not supplied a dated uptime series yet.</p>}</section><section className="executive-panel"><span className="eyebrow">STATION COMPARISON</span><h2>Operational reliability</h2>{summary?.station_comparison?.length ? <div className="station-list">{summary.station_comparison.map(station => <div key={station.station_id}><span>{station.station_id}</span><b>{station.uptime}%</b><i><em style={{ width: `${Math.max(0, Math.min(100, station.uptime))}%` }} /></i></div>)}</div> : <p className="executive-empty">No station comparison data supplied yet.</p>}</section></main>;
}
